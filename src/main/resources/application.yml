server:  port: 8010  servlet-path: /movie  application:      name: movie-service #注册到Eureka Server上的应用名eureka:  clicent:    serviceUrl:      defaultZone: http://localhost:8761/eureka/  instance:    prefer-ip-adress: true #是否将自己的IP注册到Eureka Server，false会注册微服务所在的操作系统的hostname